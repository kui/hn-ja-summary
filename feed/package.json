{
  "name": "hn-feed-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "ts:check": "npm run types && tsc --noEmit"
  },
  "dependencies": {
    "@hn-feed/shared": "*"
  }
}
