{
  "name": "hn-feed-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler d1 migrations apply hn-feed --local && wrangler dev",
    "dev:scheduled": "wrangler d1 migrations apply hn-feed --local && wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "ts:check": "npm run types && tsc --noEmit"
  },
  "dependencies": {
    "@hn-feed/shared": "*"
  }
}
